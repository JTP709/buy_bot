const bestBuyBot = require('./bestBuyBot');
const walmartBot = require('./walmartBot');
const gameStopBot = require('./gameStopBot');
const amazonBot = require ('./amazonBot');

module.exports = {
    bestBuyBot,
    walmartBot,
    gameStopBot,
    amazonBot,
}
